%span{id: "#{req.id}_req_like" }
  %span.square-badge
    = req.likes.count
  - if current_user.present? && current_user.can_like?(req)
    = link_to like_user_request_path(req.to_user, req), remote: true do
      Like
  - else
    Like

$('#user_' + <%=@user_id%>).remove();
$("#wishlist").append("<%= escape_javascript(render :partial => 'home/user_place_holder_tile') %>");
$('#member_count').html(parseInt($('#member_count').text()) - 1);
